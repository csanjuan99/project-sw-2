package com.app.medyapp.enums;

public enum ERole {
    //El aplicativo tiene dos roles:
    // ADMIN(Tiene todos los permisos CRUD) y USER(Gestiona su informacion propia)
    ROLE_ADMIN, ROLE_USER
}
